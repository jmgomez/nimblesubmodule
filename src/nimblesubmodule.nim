# This is just an example to get you started. A typical binary package
# uses this file as the main entry point of the application.
import ../sat/src/sat/satvars

let dontCare = DontCare
when isMainModule:
  echo("Hello, World!")
